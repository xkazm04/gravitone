# Session 2026-07-28-4 — round 7: propose ×2 + build 10/10

## Phase 0 — recall
- Resumed at `next: propose`, pool 0/10, round 6 complete (9/9 shipped, head `a018556`, tree clean).
- Cursor per the round-6 pointer: **TTS Playground (8)**. Prefetch: **Voice & Emotion Library (7)**.
- **Cursor reasoning**: Ingest and Concurrency Engine came off cooldown at the same nominal 8, but both had five directions shipped into them in round 5 — their headroom is freshly spent. The Playground is the context whose DEPENDENCIES moved: rounds 5-6 rewrote the exact routes it calls. Same "where is the value we already paid for but haven't delivered?" heuristic that made round 6's cursor call correct.
- Both contexts got fresh scouts, handed the round-5/6 SHAs and told to lead with a surfacing-gap audit.

## Propose — 10/10 accepted
- **Voice & Emotion Library** (never scouted at depth before): pack-import-path-safety ✅ builtin-name-collision ✅ registry-never-silently-empty ✅ registry-write-invariants ✅ one-exporter-clone-path ✅. Slate deliberately skewed to correctness (4 robustness + 1 consolidation wildcard, no feature/ux entry) because the scout surfaced DEFECTS rather than gaps; stated as such at the gate.
- **TTS Playground**: one-header-contract ✅ premium-format-in-console ✅ absent-is-not-empty ✅ console-surfaces-tested ✅ proxy-streams-audio ✅.

### Two verified defects led the round
1. **A pack import could write outside `voices/`.** `import_pack` took `emotion` from an untrusted manifest with only `.strip().lower()` and interpolated it into a filename. Director confirmed the escape before proposing: `../../../../tmp/evil` → `…/arm/tmp/evil-abc123.safetensors`, outside the directory, with the pack's own bytes. The sha256 check validates against the same manifest, and signing is off by default. (Note for future work: a URL-encoded `..%2f` does NOT escape — a naive substring ban would have been the wrong instinct.)
2. **Cloning a voice named "Mary" made it vanish.** The built-in loop in `_build_characters` ran after the cloned assembly and overwrote `chars[vid]` unconditionally; `BUILTIN` is a list of ordinary first names. 201 Created, file and row on disk, character absent from the roster.

Pleasant surprise from the Playground scout: **all five round-4 directions verified live and complete**, and two things fixed themselves — the console had been printing the concurrent-sum synth time since round 2 and now prints a true one with no web change, and a queue chip that had been dead markup for rounds started working because `/v1/speak` finally sends the header.

## Build — 5 builders (one relaunched), 3 waves, 10/10 shipped (a018556 → f580846, 12 commits)
Wave 1: **L1** registry safety · **P1** proxy layer · **P2** console. Wave 2: **L2** registry integrity (after L1). Wave 3: **L3** write invariants (after L2).

| Direction | Commit |
|---|---|
| pack-import-path-safety | `2d16fd5` |
| builtin-name-collision | `d329aec` |
| one-header-contract | `21a7064` |
| premium-format-in-console | `2400f6b` |
| proxy-streams-audio | `b69cc11` |
| absent-is-not-empty | `2531a21` |
| console-surfaces-tested | `205054a` (+ Director `a854091`) |
| registry-never-silently-empty | `401a12a` |
| one-exporter-clone-path | `3707fb4` |
| registry-write-invariants | `054a791` |
| — Director flake fix | `f580846` |

Gates at wrap: compileall clean · **540 service tests + 87 subtests** (469 at round start) · tsc clean · **191 web tests / 18 files** (139 at round start).

## Director interventions
1. **`a854091` — cross-builder break.** P1 made `synthSegments`/`format` required on `SpeakResult`; P2's new render harness predated it. Both branches green alone; on main **all 191 tests passed while tsc FAILED**, because a mocked fixture is type-checked but never executed. P2 had predicted it and left `satisfies SpeakResult` pointing at the one fixture — which is exactly where tsc sent me.
2. **`f580846` — actually removing a flake I only half-fixed in round 6.** L3 reported the batch-cap test failing under load; I stressed it and got 1 failure in 12. Round 6's fix (capacity 2→3) covered ONE lagging permit release, not a whole wave's. **I then made it worse before making it better**: I reshaped the test around `max_concurrent`, reasoning that concurrency was the timing-independent property — and my own teeth check caught that it was VACUOUS, because `max_concurrent` counts what the fake EXECUTES (bounded by its 2-worker pool), not what was admitted. It passed happily with the bug restored. Correct fix: capacity=5 — below the 6 an uncapped submission needs, three spare slots above the wave size. Teeth verified both ways, 15/15 stress runs green.
3. Verified the traversal fix against my own propose-time exploit at both layers, and teeth-checked P1's header drift guard personally (removed a header from the mirror → 3 failures; restored → 5 green).

## Builders correcting the brief — four times, all correct
- **L2**: "there is NO wall-clock saving on a direct clone" — one stem is one cold load before and after; the one-load win is per-batch and belongs to ingest. My speed claim was padding; the load-back verification was always the real value.
- **P2**: the stale rail-refs bug is **not reachable** — reverted its fix, got 0 failures, kept the keyed Map as *hardening* and said so in code, test and commit rather than claiming a fix.
- **P1**: mp3 takes **cannot be shared** (`takes.py:72` rejects non-RIFF), so it disabled share/review for mp3 with an explicit reason instead of letting users hit a 400 after rendering.
- **L3**: found beyond its brief that the manifest's dict comprehension let the LAST duplicate emotion win while `emotion_map` took the FIRST — they disagreed about which voice speaks. `_by_emotion` unifies them.

## Builder death
L3's first attempt stalled during exploration having written nothing; the worktree was clean, so there was nothing to snapshot. The relaunch summarised the orientation inline ("reuse these four helpers; read these functions, not the module") and told it to start editing early. It then delivered the round's largest single direction with six anti-vacuous reverts.

## Open follow-ups recorded
- **Teach `service/takes.py` mp3** so a performance can be shared as a small mp3 LINK, not just downloaded — the half of `premium-format-in-console` that is honestly incomplete. Cross-surface: takes storage, the `/t/{id}` player, review.
- The corrupt-registry 503 is service-wide while the file is damaged (intended, but a hard outage, one ERROR log per request); `create_voice` has a millisecond window where a voice is registered but not yet glob-visible; `shutil.move` across filesystems is not atomic.
- `/v1/voices/{id}`'s 404 still puts a dict in `detail` — but ElevenLabs' own shape IS structured, so the honest fix is to decide which contract wins and state it in `errors.py`.
- Two older registry test files still lock the REAL repo `voices/` dir (they patch `VOICES_DIR` but not the import-bound `_META_LOCK_PATH`); every test added this round uses the correct pattern.
- Still carried: the Arm A/B, the `TTS_MAX_TOKENS` truncation question, batched-Gemini label quality, and the baseline/sovereign audio checks by ear.

next: propose — round 8, cursor at **Speech Synthesis API (8.5)** and **Voice Creation Studio (8)**, both returning from cooldown; Voice Cloning & Ingest (8) and Concurrency Engine (8) are also free, as are API Key Management (6.5 — the round-4 revoke endpoint STILL has no studio affordance), Character & Voice Mgmt (6.5), Load Testing (6) and App Shell (6). TTS Playground + Voice & Emotion Library on cooldown until round 9. Re-scout anything whose brief predates 2026-07-28, and prefer a re-scout whenever a brief's FILES have moved even if its date has not.
