# Session 2026-07-28-2 — round 5: propose ×2 + build 10/10

## Phase 0 — recall
- Resumed at `next: propose`, pool 0/10, round 4 complete (10/10 shipped, head `11e3465`, tree clean).
- Cursor per the round-4 pointer: **Voice Cloning & Ingest (8)**. Prefetch: **Concurrency Engine & Metrics (8)**.
- Both briefs were stale and both got FRESH scouts. The engine re-scout was ordered by the round-4 wrap because `engine.py` changed underneath that context (Arm tuning) and `service/cache.py` is new; its scout was handed the round-4 SHAs to read first.

## Propose — 10/10 accepted, zero rejections (third round running)
- **Concurrency Engine & Metrics**: benchmark-measures-engine, segmentation-earns-its-keep, worker-death-is-visible, honest-admission-accounting, pool-truth-aggregation.
- **Voice Cloning & Ingest**: neutral-baseline-stem, input-side-seeking, cancel-stops-the-spend, external-call-budget, sovereign-path-works.

**TWO WERE ROUND-4 REGRESSIONS THE DIRECTOR SHIPPED**, surfaced by the re-scout and verified in code before presenting:
1. The synthesis cache defaults ON while `loadtest` sends one constant text and never mentions the cache — so the knee, the 429 count, `server_rtf_mean` and the SIGNED CERTIFICATE all measured an LRU lookup. Cache hits also reported `X-Realtime-Factor` = audio/1e-6 → millions, which is exactly what the knee detector thresholds on.
2. Segmentation claimed N-worker parallelism, but `workers` defaults to 1 AND `replicas.replica_env` pins `TTS_WORKERS=1` per child, so 16 units ran serially behind one worker while claiming 16 of 33 admission slots.

## Build — 6 builders, 3 waves, 10/10 shipped (11e3465 → 3bb024d, 12 commits)
Wave 1 (parallel, split by file region): **E-A** engine lifecycle · **E-B** measurement honesty · **I-A** clone quality + extraction speed.
Wave 2 (sequenced): **E-C** segmentation (after E-B) · **I-B** ingest lifecycle + external-call budget (after I-A).
Wave 3: **I-C** sovereign mode (after I-B).

| Direction | Commit |
|---|---|
| benchmark-measures-engine | `a1e2a16` |
| pool-truth-aggregation | `da5cd76` |
| worker-death-is-visible | `718a790` |
| honest-admission-accounting | `f5f0e27` |
| input-side-seeking | `ae12f52` |
| neutral-baseline-stem | `5de0b31` + Director `89769e0` |
| segmentation-earns-its-keep | `d9dd0d1` |
| cancel-stops-the-spend | `af67850` |
| external-call-budget | `05021e9` |
| sovereign-path-works | `585c816` + Director `3bb024d` |

Gates at wrap: compileall clean · **415 service tests + 28 subtests** (274 at round start) · `tsc --noEmit` clean · **76 web tests / 10 files**.

## Director interventions
1. **`89769e0`** — `plan_baseline`'s docstring claimed segments "stay in recording order"; true for the neutral ones, but borrowed top-up segments are appended after them. Corrected.
2. **`3bb024d`** — answered I-C's `DECISION NEEDED`: the sovereign toggle fires before any scan exists, so no backend field can reach it. Rewrote the web copy to mirror `ingest.SOVEREIGN_LIMITS`; the old copy omitted single-speaker entirely, so someone cloning a two-person recording had no way to know both voices become one Character until after the scan.
3. Resolved a both-append `AGG_KEYS` conflict (E-A's `cache_hits`/`collapsed` vs E-B's `audio_seconds_total` + scope constants) by union, then let the drift test confirm it.
4. Ran the cross-builder integration gate after every merge, not just at wrap (E-A alone 293, E-B alone 305, together **324** — the union is where a drift test would have fired).

## MERGE INCIDENT — Director error, caught before it stuck
I-A reported direction 1 then direction 2 but had COMMITTED them in reverse (`a911f81` seeking is the PARENT, `061dab6` baseline is the TIP). Resolving a conflict by taking `a911f81`'s file therefore silently reverted the entire baseline-stem rewrite. Caught because the commit summary read "491 deletions" for work that should have been additive; diffed main against the builder's branch, confirmed the mismatch, reset to `f5f0e27` and re-applied in true parent order, then verified byte-for-byte parity BEFORE running any gate. No bad state ever reached a green test run.

## Builders overturning their briefs — twice, both correct
- **I-C falsified this round's own premise**: the direction asserted single-pass `loudnorm` is "non-deterministic, so the local chain is not reproducible". The builder tested it — three runs, identical sha256 — measured the cost of switching anyway (1.54s vs 2.49s, 1.61×), and declined. The premise had conflated "adaptive" with "non-reproducible".
- **E-C fixed the test that caused the round-4 miss**: every batch case configured only the FakeEngine's pool while leaving `SETTINGS.workers` at 1, so the suite proved concurrency against a topology that never ships. `_chunk_every_sentence(workers)` is now a required argument and `SingleWorkerTopologyTests` asserts the shipped default outright.

## Notable finds
- **Sovereign mode would clone a voice from 12 seconds of pure digital silence** — it returned the zeros as a valid span. Now refused, with three other degenerate outcomes named honestly.
- **A cloud clone went from 40 Gemini calls to 5** (Director verified `_batches` independently: 40→5, 8→4×2 filling the pool, no index overlap).
- **The partial-Character bug is closed on both paths** — the error path needed a ledger (`on_voice`) because a raise leaves no return value; one `_rollback` now serves cancel and failure so they cannot drift.

## Operator actions this round created
1. Existing `loadtest_result.json` / `certification.json` artifacts are invalid — cache-blind and schema ≤2. `certify` now REFUSES them. Re-run on the Arm box before republishing any capacity claim.
2. Under the Linux `SO_REUSEPORT` default the launcher's aggregated `/metrics` now honestly reads `single_replica_sample` with `totals: null`. Nothing in the repo or web reads it; an external dashboard would need updating.
3. Sovereign threshold adaptation changes what gets cloned from existing quiet/noisy recordings — a re-scan produces different stems than before.

## Still unverifiable without hardware or keys (carried forward)
- Batched Gemini labelling quality vs per-segment (no API key) — run one recording through cloud mode before/after and compare `result.segments[].emotion`.
- The new baseline stem and sovereign span boundaries by EAR (no pocket-tts) — specifically whether span starts cut into the first consonant.
- The round-4 Arm tuning A/B (`bash benchmark_arm_ab.sh`) and the `TTS_MAX_TOKENS=50` vs `500` truncation question, both still open from round 4.

next: propose — round 6, cursor at **Voice Creation Studio (7.5)**, then Voice & Emotion Library (7), then API Key Management (6.5 — the round-4 revoke endpoint still has no studio affordance). Speech Synthesis API and TTS Playground come OFF cooldown this round; Ingest and Concurrency Engine are on cooldown until round 7. Note for the next Director: `app.py` and `ingest.py` both changed heavily this round, so any context whose brief predates 2026-07-28 needs a re-scout, not a banked brief.
