# Session 2026-07-28 — round 4: propose ×2 + build 10/10

## Phase 0 — recall
- Vault resumed at `next: propose`, pool 0/10, round 3 fully shipped (13/13, head `0267e77`).
- Repo moved since: `/architect` run 2026-07-26 landed 15+ commits (`21dab69` → `4413dda`) — proxy error contract, apiFetch/ErrorBanner, event-loop offload, cross-process file locks, ingest teardown protocol, abandon protocol, shared web async hooks, web test runner, cancel-a-running-generation. Tree clean at `4413dda`, branch `main`.
- **Rescore decision**: last session flagged the queue tail as thin (≤5). Round-1 contexts came off cooldown, so the cursor moved from API Key Management (opp 5) to **Speech Synthesis API (8.5)**, then **TTS Playground (8)**.
- No `.claude/active-runs.md` in this repo — ledger ritual skipped (as in rounds 1-3).

## Propose — 10/10 accepted, ZERO rejections (a first in 4 rounds)
- **Speech Synthesis API** (fresh scout): parallel-longform-tts, stream-chunk-budget, synthesis-cache, key-lifecycle-truth, arm-inference-pass. Slate deliberately all engine-depth (no UX lens) per the taste log.
- **TTS Playground** (prefetched scout, banked then used the same session): failure-truth-console, meaningful-render-progress, playground-load-path, durable-iteration-loop, reachable-characters.
- Director independently re-verified before presenting: main TTS route submits whole body as one job (app.py:469); stream submits every sentence up front vs 33 permits (app.py:560-566); rail `.slice(0,10)` (:517); stale-banner `takes.find` (:198); serialized roster await (:124-129). `max_tokens` semantics could NOT be verified (pocket-tts not installed) — became a builder REPORT ITEM instead of a claim.

## Build — 4 builders, 3 lanes, 10/10 shipped (4413dda → 11e3465)
Wave 1 concurrent: **S1** (perfect-tts-api) · **S2** (perfect-keys-arm) · **W1** (perfect-pg-truth).
**W2** (perfect-pg-loop) sequenced after W1 merged — all five playground directions live in one 857-line file.

| # | Direction | Commit |
|---|---|---|
| 1 | parallel-longform-tts | `8c4389e` + fix `2725d2a` |
| 2 | stream-chunk-budget | `ac01955` |
| 3 | synthesis-cache | `917e012` |
| 4 | key-lifecycle-truth | `233314c` |
| 5 | arm-inference-pass | `10099c4` + Director `6f2d1b0` |
| 6 | failure-truth-console | `3c47071` |
| 7 | meaningful-render-progress | `f23915d` |
| 8 | playground-load-path | `0077ade` |
| 9 | durable-iteration-loop | `ed18a56` |
| 10 | reachable-characters | `11e3465` |

Gates at wrap (on main, not in worktrees): `compileall` clean · **274 service tests + 23 subtests** (was 161 at round 3) · `tsc --noEmit` clean · **76 web tests / 10 files** (was 29/4) · `next build` compiles and prerenders every route.

## Director interventions
1. **REDO on parallel-longform-tts** — the one real defect. `_chunk_text`'s docstring claimed the unit count is bounded by `len(text)/budget` ("at most 23 units vs 33 slots"). Director reproduced the function standalone: greedy coalescing only merges when the COMBINED length fits, so 176-char sentences → 45 units, 180 → 44, 200 → 40 on an 8000-char body. Ordinary prose would 429 on a route that previously never rejected for length. Sent back with measurements + a required table-driven test. The builder rejected the Director's suggested formula with a correct argument (it only guarantees half-full units, so it tightens the bound without making it true) and used a doubling loop instead. Director re-verified the fix independently: batch units peak at 16 ≤ cap 16 ≤ window/2 across sentence lengths 5→9000, no text lost.
2. **`6f2d1b0`** — README claimed "every one of those settings is applied by default" while listing `TTS_QUANTIZE`, which is deliberately off. Docs-vs-code check, fixed inline rather than a builder round-trip.
3. Verified reachability of W1's "backend unreachable" health branch (health proxy returns `{status:"unreachable"}` on 503 — live code, not dead).
4. Resolved one both-append conflict (`config.py`: S1's `chunk_chars` vs S2's `ffmpeg_threads`) by reading the seam — both sides complete declarations, keep-both safe.

## Things the builders got right that are worth remembering
- **S2 falsified its own test**: swapped `file_lock` for `nullcontext` and got 12/36 keys surviving plus crashes, vs 36/36 with the lock. Proof the cross-process test measures something.
- **S2 claimed no speedup it did not measure** — zero real inference ran (no torch here), stated plainly, with the settling experiment named.
- **W2 found `takeStore` swallowed every failure**, which had silently made W1's just-shipped honesty banners unreachable. The payoff for sequencing rather than parallelizing.
- **Two correct builder pushbacks** (the doubling loop; `useClientReady` instead of the briefed `useMounted`, which would have broken SSR).

## Ownership changes (context-map not yet refreshed)
NEW: `service/cache.py`, `web/lib/playgroundDb.ts`, `web/lib/composerStore.ts`, `benchmark_arm_ab.sh`, plus 9 new test files. `web/lib/useMounted.ts` now also exports `useClientReady`. `service/keys.py` gained `POST /v1/keys/{kid}/revoke`.

next: propose — round 5, cursor at **Voice Cloning & Ingest (8)**. Re-scout **Concurrency Engine** rather than trusting its round-1 brief: `engine.py` changed under it this round (Arm tuning, inference context, ffmpeg thread cap). Speech Synthesis API + TTS Playground on cooldown until round 6. API Key Management raised 5 → 6.5 (the shipped revoke endpoint has no web affordance). Banked round-5 candidates and the two Arm-box experiments are listed in `Perfect.md`.
